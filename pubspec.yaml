name: budget_planner_flutter
description: "AuraBudget Pro — Planificateur Financier, Ingestion TrueLayer Open Banking & Audit Salarial IA"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  google_fonts: ^6.3.3
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^9.2.4
  google_sign_in: ^7.2.0
  webview_flutter: ^4.12.0
  http: ^1.4.0
  intl: ^0.20.2
  file_picker: ^10.1.7
  pdfx: ^2.9.2
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/
